import React from 'react';
import ReactDOM from 'react-dom/client';
import {SQLeLerning} from './sql-elerning';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <SQLeLerning />
);
